services:
  - type: web
    name: smartcond
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_main:app
    envVars:
      - key: MYSQL_HOST
        value: localhost
      - key: MYSQL_USER
        value: root
      - key: MYSQL_PASSWORD
        value: ""
      - key: MYSQL_DATABASE
        value: condominio_db